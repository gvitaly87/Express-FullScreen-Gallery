import populateGallery from './populate-gallery.mjs';
import animateGallery from './animate-gallery.mjs';

populateGallery(); // Inserts the images from the object array into the webpage
animateGallery(); // Takes care of animating the gallery
